#включить<iostream>
#включить <math.h>
использование пространства имен std;
двойная задача 1(int x, int n, плавающая и последняя) {
 результат с плавающей точкой = 1;
 последнее = 1;
 для (int i = 1; i
	{
 последний = -1 * последний*(2 * i - 1) / (2 * i);
 результат += последний;
	}
 возвращаемый результат;
}

двойная задача 2(int x, float E, float &N)
{
 int i;
 N = 0;
 последнее плавающее значение = 1;
 двойной результат = 0;
 для (i = 1; fabs(последний) > E; ++i)
	{
 последний = -1 * последний*(2 * i - 1) / (2 * i);
 результат += последний;
 N++;
	}
 возвращаемый результат;
}
int main()
{
 setlocale(LC_ALL, "RUS");
 int n;
 последнее плавающее значение = 1;
 int x;
 cout << "введите значение аргумента" << endl;
 cin >>> x;
 cout << "введите количество чисел" << endl;
 cin >>> n;



 плавающий otv = 1 / sqrt(1 + x);
 поплавок otv2 = задача 1(x, n, последняя);
 cout << "Точное значение функции равно " << otv << endl;
 cout << "Частичная сумма ряда равна:" << otv2 << endl;
 cout << "погрешность" << abs(otv2 - otv) << endl;
 cout << "последний член ряда:" << last << endl;
 если (abs(otv2 - otv) > abs(последний))
 cout << "погрешность больше последнего слагаемого" << endl;
 ещё
 cout << "погрешность меньше последнего слагаемого" << endl;

 cout << "Задание 2" << endl;
 двойное Е;
 cout << "Введите значение аргумента > ";
 cin >>> x;
 cout << "Задайте точность вычислений > ";
 cin >> E;
 число с плавающей точкой N = 0;
 функция с плавающей точкой = 1 / sqrt(1 + x);
 сумма с плавающей запятой = задача 2(x, E, N);
 cout << "Точное значение функции равно " << func << endl;
 cout << "Частичная сумма ряда c заданной точностью равна " << sum << endl;
 cout << "Учтено " << N << " членов ряда" << endl;
 сумма = задача 2(x, E / 10, N);
 cout << "Частичная сумма ряда c точностью, большей в 10 раз, равна " << sum << endl;
 cout << "Учтено " << N << " членов ряда" << endl;
 getchar();
 возврат 0;
}
